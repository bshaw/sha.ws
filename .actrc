# use the full image
--platform ubuntu-latest=ghcr.io/catthehacker/ubuntu:act-20.04

# configure secrets for gsutil (google-cloud-storage.yml)
--secret GCS_PROJECT
--secret GCS_SA_KEY
--secret GCS_BUCKET

# configure secret for ghcr (ghcr.yml)
--secret CR_PAT

# configure secrets for web server deployment (web-server.yml)
--secret SSH_HOST
--secret SSH_USER
--secret SSH_KEY
--secret SSH_KNOWN_HOSTS
--secret SERVER_PATH
